--- a/init/Android.bp
+++ b/init/Android.bp
@@ -112,8 +112,41 @@
     ],
 }
 
+soong_config_module_type {
+    name: "andrix_delegated_flags_type",
+    module_type: "cc_defaults",
+    config_namespace: "andrix",
+    bool_variables: ["delegated_service"],
+    properties: ["cflags", "header_libs"],
+}
+andrix_delegated_flags_type {
+    name: "andrix_delegated_flags",
+    soong_config_variables: {
+        delegated_service: {
+            cflags: ["-DANDRIX_DELEGATED_SERVICE_PROOF=1"],
+            header_libs: ["andrix_delegated_init_headers"],
+        },
+    },
+}
+soong_config_module_type {
+    name: "andrix_delegated_sources_type",
+    module_type: "cc_defaults",
+    config_namespace: "andrix",
+    bool_variables: ["delegated_service"],
+    properties: ["srcs", "static_libs"],
+}
+andrix_delegated_sources_type {
+    name: "andrix_delegated_sources",
+    soong_config_variables: {
+        delegated_service: {
+            srcs: [":andrix_delegated_init_sources"],
+            static_libs: ["andrix_delegated_scope"],
+        },
+    },
+}
 libinit_cc_defaults {
     name: "init_defaults",
+    defaults: ["andrix_delegated_flags"],
     sanitize: {
         misc_undefined: ["signed-integer-overflow"],
     },
@@ -239,6 +272,7 @@
     defaults: [
         "init_defaults",
         "selinux_policy_version",
+        "andrix_delegated_sources",
     ],
     srcs: init_common_sources + init_device_sources,
     export_include_dirs: ["."],
@@ -664,7 +698,7 @@
 
 cc_library_static {
     name: "libinit_test_utils",
-    defaults: ["libinit_test_utils_libraries_defaults"],
+    defaults: ["libinit_test_utils_libraries_defaults", "andrix_delegated_flags", "andrix_delegated_sources"],
     cflags: [
         "-Wall",
         "-Wextra",
@@ -702,6 +736,7 @@
 
 cc_defaults {
     name: "init_host_defaults",
+    defaults: ["andrix_delegated_flags", "andrix_delegated_sources"],
     host_supported: true,
     cflags: [
         "-Wall",
@@ -829,3 +864,12 @@
     aconfig_declarations: "init_flags",
     host_supported: true,
 }
+
+// Parser/definition construction only. This target does not start a service.
+cc_test_host {
+    name: "andrix_delegated_profile_test",
+    defaults: ["init_host_defaults"],
+    compile_multilib: "first",
+    srcs: [":andrix_delegated_profile_test_sources"],
+    static_libs: ["libinit_host"],
+}
--- a/init/service.h
+++ b/init/service.h
@@ -38,6 +38,9 @@
 #include "parser.h"
 #include "service_utils.h"
 #include "subcontext.h"
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+#include "delegated_service.h"
+#endif
 
 #define SVC_DISABLED 0x001        // do not autostart with class
 #define SVC_ONESHOT 0x002         // do not restart on exit
@@ -65,8 +68,17 @@
 namespace android {
 namespace init {
 
+struct DelegatedInstance;
+struct DelegatedProfile {
+    uint64_t memory = 0;
+    uint64_t swap = 0;
+    uint32_t descendants = 0;
+    uint32_t depth = 0;
+};
 class Service {
     friend class ServiceParser;
+    friend class DelegatedService;
+    friend struct DelegatedInstance;
 
   public:
     Service(const std::string& name, Subcontext* subcontext_for_restart_commands,
@@ -129,7 +141,7 @@
     int priority() const { return proc_attr_.priority; }
     int oom_score_adjust() const { return oom_score_adjust_; }
     bool is_override() const { return override_; }
-    bool process_cgroup_empty() const { return process_cgroup_empty_; }
+    bool process_cgroup_empty() const;
     unsigned long start_order() const { return start_order_; }
     void set_sigstop(bool value) { sigstop_ = value; }
     std::chrono::seconds restart_period() const {
@@ -226,6 +238,9 @@
     std::string limit_property_;
 
     bool process_cgroup_empty_ = false;
+    // Stable layout for every Service consumer in the selected source tree.
+    std::optional<DelegatedProfile> delegation_profile_;
+    std::shared_ptr<DelegatedInstance> delegation_instance_;
 
     bool override_ = false;
 
--- a/init/service.cpp
+++ b/init/service.cpp
@@ -233,7 +233,16 @@
     }
 }
 
+bool Service::process_cgroup_empty() const {
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+    if (delegation_profile_) return DelegatedService::Empty(*this);
+#endif
+    return process_cgroup_empty_;
+}
 void Service::KillProcessGroup(int signal) {
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+    if (DelegatedService::Signal(*this, signal)) return;
+#endif
     // Always attempt the process kill if process is still running.
     // Cgroup clean up routines are idempotent. It's safe to call
     // killProcessGroup repeatedly. During shutdown, `init` will
@@ -295,6 +304,9 @@
 }
 
 void Service::Reap(const siginfo_t& siginfo) {
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+    if (DelegatedService::DeferReap(*this, siginfo)) return;
+#endif
     if (!(flags_ & SVC_ONESHOT) || (flags_ & SVC_RESTART)) {
         KillProcessGroup(SIGKILL);
     } else {
@@ -460,6 +472,9 @@
 
 
 Result<void> Service::ExecStart() {
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+    if (delegation_profile_) return Error() << "delegated service exec waits are not qualified";
+#endif
     auto reboot_on_failure = make_scope_guard([this] {
         if (on_failure_reboot_target_) {
             trigger_shutdown(*on_failure_reboot_target_);
@@ -606,6 +621,9 @@
                                  // Apply the task profiles to the current process.
                                  SetProcessProfiles(getuid(), getpid(), task_profiles_);
         if (!succeeded) {
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+            if (delegation_profile_) _exit(126);
+#endif
             LOG(ERROR) << "failed to set task profiles";
         }
     }
@@ -621,6 +639,9 @@
 }
 
 Result<void> Service::Start() {
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+    OR_RETURN(DelegatedService::BeforeStart(*this));
+#endif
     auto reboot_on_failure = make_scope_guard([this] {
         if (on_failure_reboot_target_) {
             trigger_shutdown(*on_failure_reboot_target_);
@@ -728,6 +749,9 @@
         }
     }
 
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+    OR_RETURN(DelegatedService::Prepare(*this, descriptors));
+#endif
     pid_t pid = -1;
     if (namespaces_.flags) {
         pid = clone(nullptr, nullptr, namespaces_.flags | SIGCHLD, nullptr);
@@ -747,6 +771,9 @@
     }
 
     if (pid < 0) {
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+        DelegatedService::NoProcess(*this);
+#endif
         pid_ = 0;
         return ErrnoError() << "Failed to fork";
     }
@@ -767,6 +794,17 @@
     start_order_ = next_start_order_++;
     process_cgroup_empty_ = false;
 
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+    if (delegation_profile_) {
+        auto assigned = DelegatedService::Assign(*this, pid_);
+        if (!assigned.ok()) {
+            DelegatedService::SetupFailed(*this);
+            auto notified = cgroups_activated.Write(kActivatingCgroupsFailed);
+            if (!notified.ok()) return notified.error();
+            return assigned.error();
+        }
+    } else
+#endif
     if (CgroupsAvailable()) {
         bool use_memcg = swappiness_ != -1 || soft_limit_in_bytes_ != -1 || limit_in_bytes_ != -1 ||
                          limit_percent_ != -1 || !limit_property_.empty();
@@ -799,6 +837,9 @@
     }
 
     if (Result<void> result = cgroups_activated.Write(kCgroupsActivated); !result.ok()) {
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+        DelegatedService::SetupFailed(*this);
+#endif
         return Error() << "Sending cgroups activated notification failed: " << result.error();
     }
 
@@ -832,6 +873,9 @@
     setsid_finished.Close();
 
     NotifyStateChange("running");
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+    if (delegation_profile_) DelegatedService::Pump();
+#endif
     reboot_on_failure.Disable();
 
     LOG(INFO) << "... started service '" << name_ << "' has pid " << pid_;
@@ -969,7 +1013,11 @@
 void Service::Terminate() {
     flags_ &= ~(SVC_RESTARTING | SVC_DISABLED_START);
     flags_ |= SVC_DISABLED;
-    if (pid_) {
+    if (pid_
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+        || DelegatedService::Pending(*this)
+#endif
+    ) {
         KillProcessGroup(SIGTERM);
         NotifyStateChange("stopping");
     }
@@ -981,14 +1029,22 @@
     // all other processes will transition to be restarting.
     LOG(INFO) << "Service '" << name_ << "' expired its timeout of " << timeout_period_->count()
               << " seconds and will now be killed";
-    if (pid_) {
+    if (pid_
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+        || DelegatedService::Pending(*this)
+#endif
+    ) {
         KillProcessGroup(SIGKILL);
         NotifyStateChange("stopping");
     }
 }
 
 void Service::Restart() {
-    if (flags_ & SVC_RUNNING) {
+    if ((flags_ & SVC_RUNNING)
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+        || DelegatedService::Pending(*this)
+#endif
+    ) {
         /* Stop, wait, then start the service. */
         StopOrReset(SVC_RESTART);
     } else if (!(flags_ & SVC_RESTARTING)) {
@@ -1026,7 +1082,11 @@
         flags_ &= (~SVC_RESTART);
     }
 
-    if (pid_) {
+    if (pid_
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+        || DelegatedService::Pending(*this)
+#endif
+    ) {
         if (flags_ & SVC_GENTLE_KILL) {
             KillProcessGroup(SIGTERM);
             if (!process_cgroup_empty()) std::this_thread::sleep_for(200ms);
--- a/init/service_parser.h
+++ b/init/service_parser.h
@@ -41,6 +41,9 @@
     const KeywordMap<ServiceParser::OptionParser>& GetParserMap() const;
 
     Result<void> ParseCapabilities(std::vector<std::string>&& args);
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+    Result<void> ParseDelegatedScope(std::vector<std::string>&& args);
+#endif
     Result<void> ParseClass(std::vector<std::string>&& args);
     Result<void> ParseConsole(std::vector<std::string>&& args);
     Result<void> ParseCritical(std::vector<std::string>&& args);
--- a/init/service_parser.cpp
+++ b/init/service_parser.cpp
@@ -581,6 +581,11 @@
     return {};
 }
 
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+Result<void> ServiceParser::ParseDelegatedScope(std::vector<std::string>&& args) {
+    return DelegatedService::Configure(*service_, args);
+}
+#endif
 const KeywordMap<ServiceParser::OptionParser>& ServiceParser::GetParserMap() const {
     constexpr std::size_t kMax = std::numeric_limits<std::size_t>::max();
     // clang-format off
@@ -590,6 +595,9 @@
         {"console",                 {0,     1,    &ServiceParser::ParseConsole}},
         {"critical",                {0,     2,    &ServiceParser::ParseCritical}},
         {"disabled",                {0,     0,    &ServiceParser::ParseDisabled}},
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+        {"delegated_scope",         {4,     4,    &ServiceParser::ParseDelegatedScope}},
+#endif
         {"enter_namespace",         {2,     2,    &ServiceParser::ParseEnterNamespace}},
         {"file",                    {2,     2,    &ServiceParser::ParseFile}},
         {"gentle_kill",             {0,     0,    &ServiceParser::ParseGentleKill}},
@@ -700,8 +708,14 @@
         }
     }
 
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+    OR_RETURN(DelegatedService::Validate(*service_));
+#endif
     Service* old_service = service_list_->FindService(service_->name());
     if (old_service) {
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+        if (!DelegatedService::Removable(*old_service)) return Error() << "delegated instance still owns resources";
+#endif
         if (!service_->is_override()) {
             return Error() << "ignored duplicate definition of service '" << service_->name()
                            << "'";
--- a/init/service_list.h
+++ b/init/service_list.h
@@ -40,8 +40,12 @@
     void RemoveService(const Service& svc);
     template <class UnaryPredicate>
     void RemoveServiceIf(UnaryPredicate predicate) {
-        services_.erase(std::remove_if(services_.begin(), services_.end(), predicate),
-                        services_.end());
+        services_.erase(std::remove_if(services_.begin(), services_.end(), [&](const auto& service) {
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+            if (!DelegatedService::Removable(*service)) return false;
+#endif
+            return predicate(service);
+        }), services_.end());
     }
 
     template <typename T, typename F = decltype(&Service::name)>
--- a/init/service_list.cpp
+++ b/init/service_list.cpp
@@ -52,6 +52,9 @@
 }
 
 void ServiceList::RemoveService(const Service& svc) {
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+    if (!DelegatedService::Removable(svc)) { LOG(ERROR) << "refusing removal of owned delegated instance"; return; }
+#endif
     auto svc_it = std::find_if(
             services_.begin(), services_.end(),
             [&svc](const std::unique_ptr<Service>& s) { return svc.name() == s->name(); });
--- a/init/sigchld_handler.cpp
+++ b/init/sigchld_handler.cpp
@@ -69,7 +69,14 @@
     // whenever the function returns from this point forward.
     // We do NOT want to reap the zombie earlier as in Service::Reap(), we kill(-pid, ...) and we
     // want the pid to remain valid throughout that (and potentially future) usages.
-    auto reaper = make_scope_guard([pid] { TEMP_FAILURE_RETRY(waitpid(pid, nullptr, WNOHANG)); });
+    auto reaper = make_scope_guard([pid] {
+        const auto reaped = TEMP_FAILURE_RETRY(waitpid(pid, nullptr, WNOHANG));
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+        if (reaped == pid) DelegatedService::Reaped(pid);
+#else
+        (void)reaped;
+#endif
+    });
 
     std::string name;
     std::string wait_string;
@@ -77,6 +84,10 @@
 
     if (SubcontextChildReap(pid)) {
         name = "Subcontext";
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+    } else if (DelegatedService::WorkerExited(pid, siginfo)) {
+        name = "Delegated cleanup worker";
+#endif
     } else {
         service = ServiceList::GetInstance().FindService(pid, &Service::pid);
 
@@ -123,7 +134,11 @@
 
     service->Reap(siginfo);
 
-    if (service->flags() & SVC_TEMPORARY) {
+    if ((service->flags() & SVC_TEMPORARY)
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+        && DelegatedService::Removable(*service)
+#endif
+    ) {
         ServiceList::GetInstance().RemoveService(*service);
     }
 
@@ -142,6 +157,9 @@
 }
 
 static void ReapAndRemove(std::vector<pid_t>& alive_pids) {
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+    DelegatedService::Pump();
+#endif
     for (auto pid : ReapAnyOutstandingChildren()) {
         const auto it = std::find(alive_pids.begin(), alive_pids.end(), pid);
         if (it != alive_pids.end()) {
@@ -181,9 +199,17 @@
     }
     std::vector<pid_t> alive_pids(pids);
     ReapAndRemove(alive_pids);
-    while (!alive_pids.empty() && t.duration() < timeout) {
+    while ((!alive_pids.empty()
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+            || DelegatedService::AnyStopping()
+#endif
+           ) && t.duration() < timeout) {
         if (sigchld_fd >= 0) {
-            auto result = epoll.Wait(std::max(timeout - t.duration(), 0ms));
+            auto remaining = std::max(timeout - t.duration(), 0ms);
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+            if (DelegatedService::AnyStopping()) remaining = std::min(remaining, 50ms);
+#endif
+            auto result = epoll.Wait(remaining);
             if (result.ok()) {
                 ReapAndRemove(alive_pids);
                 continue;
--- a/init/init.cpp
+++ b/init/init.cpp
@@ -409,6 +409,9 @@
             }
         }
 
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+        if (DelegatedService::Pending(*s)) continue;
+#endif
         if (!(s->flags() & SVC_RESTARTING)) continue;
 
         auto restart_time = s->time_started() + s->restart_period();
@@ -1152,6 +1155,9 @@
 
     InstallSignalFdHandler(&epoll);
     InstallInitNotifier(&epoll);
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+    DelegatedService::Install(epoll, [] { WakeMainInitThread(); });
+#endif
     StartPropertyService(&property_fd);
 
     // If boot_timeout property has been set in a debug build, start the boot monitor
--- a/init/main.cpp
+++ b/init/main.cpp
@@ -20,6 +20,9 @@
 #include "selinux.h"
 #include "subcontext.h"
 #include "ueventd.h"
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+#include "delegated_service.h"
+#endif
 
 #include <android-base/logging.h>
 
@@ -63,6 +66,9 @@
     }
 
     if (argc > 1) {
+#if defined(ANDRIX_DELEGATED_SERVICE_PROOF) && !defined(FIRST_STAGE_INIT) && !defined(RECOVERY)
+        if (!strcmp(argv[1], "delegated_cleanup")) return DelegatedService::WorkerMain(argc, argv);
+#endif
         if (!strcmp(argv[1], "subcontext")) {
             android::base::InitLogging(argv, &android::base::KernelLogger);
             const BuiltinFunctionMap& function_map = GetBuiltinFunctionMap();
--- a/init/builtins.cpp
+++ b/init/builtins.cpp
@@ -1296,6 +1296,18 @@
 }
 
 // Builtin-function-map start
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+static Result<void> do_stop_service_instance(const BuiltinArguments& args) {
+    auto* service = ServiceList::GetInstance().FindService(args[1]);
+    if (!service) return Error() << "no declared service";
+    return DelegatedService::StopExact(*service, args[2]);
+}
+static Result<void> do_service_worker_fault(const BuiltinArguments& args) {
+    auto* service = ServiceList::GetInstance().FindService(args[1]);
+    if (!service) return Error() << "no declared service";
+    return DelegatedService::WorkerFault(*service, args[2], args[3]);
+}
+#endif
 const BuiltinFunctionMap& GetBuiltinFunctionMap() {
     constexpr std::size_t kMax = std::numeric_limits<std::size_t>::max();
     // clang-format off
@@ -1348,6 +1360,10 @@
         {"setprop",                 {2,     2,    {true,   do_setprop}}},
         {"setrlimit",               {3,     3,    {false,  do_setrlimit}}},
         {"start",                   {1,     1,    {false,  do_start}}},
+#ifdef ANDRIX_DELEGATED_SERVICE_PROOF
+        {"stop_service_instance",   {2,     2,    {false,  do_stop_service_instance}}},
+        {"service_worker_fault",    {3,     3,    {false,  do_service_worker_fault}}},
+#endif
         {"stop",                    {1,     1,    {false,  do_stop}}},
         {"swapon_all",              {0,     1,    {false,  do_swapon_all}}},
         {"swapoff",                 {1,     1,    {false,  do_swapoff}}},
