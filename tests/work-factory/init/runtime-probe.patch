--- a/init/service.h
+++ b/init/service.h
@@ -60,6 +60,10 @@
 #define SVC_GENTLE_KILL 0x2000  // This service should be stopped with SIGTERM instead of SIGKILL
                                 // Will still be SIGKILLed after timeout period of 200 ms
 
+#ifdef ANDRIX_FACTORY_INIT
+#define SVC_ANDRIX_FACTORY_PROOF 0x4000
+#endif
+
 #define NR_SVC_SUPP_GIDS 32    // thirty two supplementary groups
 
 namespace android {
@@ -67,6 +71,7 @@
 
 class Service {
     friend class ServiceParser;
+    friend class WorkProfileExperiment;
 
   public:
     Service(const std::string& name, Subcontext* subcontext_for_restart_commands,
--- a/init/service_list.h
+++ b/init/service_list.h
@@ -29,6 +29,7 @@
 namespace init {
 
 class ServiceList {
+    friend class WorkProfileExperiment;
   public:
     static ServiceList& GetInstance();
 
--- a/init/service.cpp
+++ b/init/service.cpp
@@ -15,6 +15,9 @@
  */
 
 #include "service.h"
+#ifdef ANDRIX_FACTORY_INIT
+#include "work_profile_experiment.h"
+#endif
 
 #include <errno.h>
 #include <fcntl.h>
@@ -798,6 +801,16 @@
         }
     }
 
+#ifdef ANDRIX_FACTORY_INIT
+    if ((flags_ & SVC_ANDRIX_FACTORY_PROOF) != 0) {
+        if (auto result = WorkProfileExperiment::ActivateForInit(*this); !result.ok()) {
+            auto notified = cgroups_activated.Write(kActivatingCgroupsFailed);
+            if (!notified.ok()) return notified.error();
+            return result.error();
+        }
+    }
+#endif
+
     if (Result<void> result = cgroups_activated.Write(kCgroupsActivated); !result.ok()) {
         return Error() << "Sending cgroups activated notification failed: " << result.error();
     }
--- a/init/builtins.cpp
+++ b/init/builtins.cpp
@@ -104,6 +104,9 @@
 
 namespace android {
 namespace init {
+#ifdef ANDRIX_FACTORY_INIT
+Result<void> do_andrix_factory_start(const BuiltinArguments& args);
+#endif
 
 // There are many legacy paths in rootdir/init.rc that will virtually never exist on a new
 // device, such as '/sys/class/leds/jogball-backlight/brightness'.  As of this writing, there
@@ -1314,6 +1317,9 @@
         {"enable",                  {1,     1,    {false,  do_enable}}},
         {"exec",                    {1,     kMax, {false,  do_exec}}},
         {"exec_background",         {1,     kMax, {false,  do_exec_background}}},
+#ifdef ANDRIX_FACTORY_INIT
+        {"andrix_factory_start",     {1,     1,    {false,  do_andrix_factory_start}}},
+#endif
         {"exec_start",              {1,     1,    {false,  do_exec_start}}},
         {"export",                  {2,     2,    {false,  do_export}}},
         {"hostname",                {1,     1,    {true,   do_hostname}}},
--- a/init/Android.bp
+++ b/init/Android.bp
@@ -112,8 +112,27 @@
     ],
 }
 
+soong_config_module_type {
+    name: "andrix_init_factory_defaults_type",
+    module_type: "cc_defaults",
+    config_namespace: "andrix",
+    bool_variables: ["factory_init"],
+    properties: ["cflags", "srcs", "include_dirs"],
+}
+andrix_init_factory_defaults_type {
+    name: "andrix_init_factory_defaults",
+    soong_config_variables: {
+        factory_init: {
+            cflags: ["-DANDRIX_FACTORY_INIT=1"],
+            srcs: [":andrix_factory_init_runtime_sources"],
+            include_dirs: ["vendor/andrix/tests/work-factory/init"],
+        },
+    },
+}
+
 libinit_cc_defaults {
     name: "init_defaults",
+    defaults: ["andrix_init_factory_defaults"],
     sanitize: {
         misc_undefined: ["signed-integer-overflow"],
     },
