<?xml version="1.0" encoding="UTF-8"?>
<!-- This is intentionally not an installation manifest. The Andrix source is
     https://github.com/nicholasraimbault/andrix. For Phase 1, place an exact
     committed checkout at vendor/andrix (a symlink to that checkout suffices).
     Pin an explicit commit before adding a <project>; do not silently track
     a moving branch. An empty manifest is not build provenance. -->
<manifest>
</manifest>
